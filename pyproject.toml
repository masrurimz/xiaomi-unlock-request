[project]
name = "xiaomi-unlock-request"
version = "1.0.0"
description = "Xiaomi bootloader unlock request — AQLR method, NTP-synced to midnight Beijing time"
requires-python = ">=3.10"
dependencies = [
    "ntplib>=0.4.0",
    "httpx>=0.27.0",
]
